import { connect } from 'react-redux';

import { openModal, closeModal } from '../../actions/image_modal_actions';

const mSTP = state => ({

})

const mDTP = dispatch => ({

})

export default connect(mSTP, mDTP)(CreateMemes)